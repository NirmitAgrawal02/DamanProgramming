start
    int first = 0;
    int second = 1;
    print(first);
    print(second);
    for(int i = 0; i < 5; i = i + 1)
        begin
            int y = first + second;
            first = second;
            second = y;
            print(y)
        end;
finish .